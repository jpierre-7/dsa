# Mission now names seven core patterns explicitly

User asked for the curriculum to include two pointers, sliding window, fast & slow pointers, binary search (plus variations), DFS & BFS, dynamic programming, and backtracking. These are now in [[MISSION.md]] as success criteria and are sequenced in the roadmap in `NOTES.md`.

**Implications:** prerequisites have to come first: linked lists before fast & slow pointers, recursion before trees/DFS, and backtracking before DP (memoized recursion). Foundations lessons should stay short so the patterns arrive soon.
